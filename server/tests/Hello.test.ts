/**
 * Dependencies
 */

const request = require('request');
const app = require('../index');

/**
 * Assertions
 */

describe("Hello", () => {
  it('has tests', () => {
    expect(1 + 1).toBe(2)
  })
});
